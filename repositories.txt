# the first line is the default branch for when our branch does not exist in a repo
master

# the remaining lines are a list repositories that should get tagged
openxtwin
xc-windows
msi-installer
win-tools
idl
xc-vusb
